
#first target in the makefile is the default target
#override this behavior using a special phony target
# .DEFAULT_GOAL := generate 

display:
	@echo "Welcome"
generate:
	@echo "Creating empty text files..."
	touch file.txt

clean:
	@echo "Cleaning up..."
	rm *.txt
